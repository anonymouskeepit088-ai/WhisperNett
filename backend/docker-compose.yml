version: '3.8'

services:
  whispernet-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whispernet-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
